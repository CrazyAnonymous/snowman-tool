<config>
    <jars>
        <!-- <jar in="${project.build.finalName}.jar" out="${project.build.finalName}-obfuscated.jar"/> -->
        <jar in="${project.build.finalName}.jar" out="${project.build.finalName}.jar"/>
    </jars>
    
    <keep-names>
        <class access="protected+">
            <field access="protected+"/>
            <method access="protected+"/>
        </class>
        
        <!-- <ignore-classes>
        	<class template="class org.snowman.tool.model.PojoUtils"/>
        </ignore-classes> -->
    </keep-names>

    <property name="log-file" value="log.xml"/>
</config>
